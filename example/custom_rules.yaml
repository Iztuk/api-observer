rules:
  request.admin_endpoint_accessed:
    enabled: true
    applies_to:
      - request
    type: path
    description: Detect when an admin endpoint is accessed.

    match:
      patterns:
        - target: path
          pattern: "/admin/*"

    finding:
      title: Admin endpoint accessed
      message: Administrative endpoint was requested.
